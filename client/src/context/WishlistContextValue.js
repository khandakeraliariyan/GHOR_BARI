import { createContext } from 'react';

// Export only the context value from this file to keep HMR/fast-refresh happy
export const WishlistContext = createContext();

export default WishlistContext;
